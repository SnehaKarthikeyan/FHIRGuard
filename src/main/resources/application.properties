spring.application.name=FHIRGuard

server.port=8080

# false = completely local mode
# true  = use OpenAI for AI agents
fhirguard.llm-enabled=false

# Only used when LLM is enabled
fhirguard.open-ai-model=gpt-5.6